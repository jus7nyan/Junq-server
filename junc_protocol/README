# На счет from и to:
#   скорее всего from не нужно (?)
#   возможно to == server означает его обработку сервером без дальнейшего отправления другому клиенту (только обратная связь)
#   to == {another user's nickname} означает прямую перессылку пользователю без обработки сервером (максимум добавление from с заменой на nick)
#   to == {room name} означает массовую рассылку всем пользователям зарегестрированных в комнате (from заменяется на название комнаты и ник отправителя сообщения)


# Сообщения от сервера

NS Message # new session message

msg: {from}<~${to}<~${type}
Отправляется клиенту при его подключении к серверу
Означает что сервер готов принять LM Message


XM Message # exit message

EM

msg: {from}<~${to}<~${type}<~${request}
Отправляется сервером при ошибке выполнения запроса от клиента
означает что возможно нужно повторно попытаться выполнить операцию но с другим запросом (request)



# Сообщения от клиента

LM Message # login message

msg: {to}<~${type}<~$(nick)
Отправляется клиентом при его желании залогинится
означает что ему будет соответствовать его уникальный ник при 


PM Message # Private Message

msg: {to}<~${type}<~${request}
Отправляется 